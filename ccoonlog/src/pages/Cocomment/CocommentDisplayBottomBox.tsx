import CocommentInput from '../../components/cocomment/CocommentInput';
import { Comment as S } from '../../styles/styles';

const CocommentDisplayBottomBox = () => {
  return (
    <S.CommentDisplayBottomBox>
      <CocommentInput />
    </S.CommentDisplayBottomBox>
  );
};

export default CocommentDisplayBottomBox;
